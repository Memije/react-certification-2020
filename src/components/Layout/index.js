import React from "react";

import Grid from "../Grid";
import Header from "../Header";

const Layout = () => {
  return (
    <div>
      <Header />
      <Grid />
    </div>
  );
};

export default Layout;
